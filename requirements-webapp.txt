# The Sentinel — runtime dependencies for the full system (API + scheduler).
#
# Install with:
#     pip install -r requirements-webapp.txt
#
# Pins are kept loose so wheels are available on current Python (3.13).
# Re-pin with `pip freeze` once you decide to lock for deployment.

# --- API (sentinel_api.py) -------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# --- Scheduler (scheduler.py) ----------------------------------------------
croniter>=2.0

# --- Music feature (onboard.py, spotify_handler.py — feature-music branch) -
requests>=2.31
